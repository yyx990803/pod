var args = require('optimist').argv._,
    fs = require('fs'),
    exec = require('child_process').exec

var command = args[0]

if (tasks[command]) {
    tasks[command](args[1], args[2])
} else {
    console.log('do: command not found')
}

var tasks = {

    create: function (appname) {

    },

    proxy: function (port, domain) {

    },

    repo: function (appname) {

    },

    sethome: function (path) {

    },

    setsrv: function (path) {

    }

}